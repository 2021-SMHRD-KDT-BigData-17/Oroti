<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="jisang.poyong.mapper.Mapper">
	<!-- 유저 회원가입 -->
	<insert id="SignUp" parameterType="jisang.poyong.vo.UserVO">
		insert into user_info
		values(
		#{user_id},
		#{user_pw},
		#{user_name},
		#{user_address},
		null,
		A,
		#{user_obstc_code},
		null);
	</insert>
	
	<!-- 로그인 -->
	<select id="SignIn" resultType="jisang.poyong.vo.UserVO">
		select user_id, user_pw 
		  from user_info
		 where user_id = #{user_id}
		   and user_pw = #{user_pw}   
	</select>
</mapper>